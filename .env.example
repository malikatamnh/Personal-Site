PORT=3000

# PostgreSQL connection
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=cars_db

# Optional
# PGSSL=true
# PGPOOL_MAX=10
# PG_IDLE_TIMEOUT_MS=30000

# JWT
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

